# domain

Workspace candidate context. Pure values, invariants and transitions. No host, IO, application or peer-context imports.

The revision 1 boundary is specified in [../CONTRACT.md](../CONTRACT.md).
Runtime implementation is still reserved for the first consuming slice. Add
only the files needed by that slice; this directory is not yet an implemented
capability.
