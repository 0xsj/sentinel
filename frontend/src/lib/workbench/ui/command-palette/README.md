# command-palette

`CommandPalette` filters a supplied command list, supports arrow-key navigation
and Enter execution, and reports the selected command through a callback. The
registry and dispatch contract remain owned by the workbench host.
