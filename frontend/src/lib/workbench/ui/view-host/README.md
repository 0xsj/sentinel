# view-host

`ViewHost` renders explicitly registered views through the shared tab primitive
and keeps active view state controlled by its host. View registration and
application routing remain outside this UI component.
