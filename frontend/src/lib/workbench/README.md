# workbench

Reusable frontend workbench composition. Depends on components and runtime state;
never imports features. `WorkbenchShell` and `WorkbenchPanel` provide the first
layout slice while root-level registration remains a later concern.
