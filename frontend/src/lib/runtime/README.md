# runtime

Portable interaction state plus explicit Svelte bindings. Theme and density are
frontend-only preferences applied as root data attributes and persisted locally;
components consume the resulting token overrides rather than receiving density
props. Keep future subscriptions scoped and disposable.
