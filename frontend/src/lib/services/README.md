# services

Typed application operations. Receive capabilities through arguments; validate responses and return Results. No adapter construction or Svelte imports.

Reserved scaffold; no runtime implementation yet. Add only the files needed by
the first consuming slice. This directory is not an implemented capability.
