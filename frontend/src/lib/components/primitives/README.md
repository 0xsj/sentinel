# primitives

Foundational visual components. `Button`, `IconButton`, `Surface`, `Separator`,
`Text`, `Heading`, `Label` and `Kbd` are live primitives; they read semantic
tokens and own only presentation concerns. Complex interaction primitives wrap
Bits UI as they are introduced.
