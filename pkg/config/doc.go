// Package config reads captured environment values into validated configuration.
// Root owns settings; this reader owns presence, parsing, safe diagnostics and
// redacted manifests. See CONTRACT.md for V01-V10 and exact parsing rules.
package config
